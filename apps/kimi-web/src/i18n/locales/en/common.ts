export default {
  /** Shared title of the right-side panel — both occupants are previews. */
  preview: 'Preview',
  loading: 'Loading',
  /** Generic confirm / cancel button labels (used by ConfirmDialog). */
  confirm: 'Confirm',
  cancel: 'Cancel',
  /** Dialog header close control (upstream localizes it). */
  close: 'Close',
  /** Code-block header actions (0.39 code-block-interaction port). */
  copy: 'Copy',
  toggleLineNumbers: 'Toggle line numbers',
  toggleWordWrap: 'Toggle word wrap',
} as const;
