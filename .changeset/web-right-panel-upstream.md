---
"@moonshot-ai/kimi-code": patch
---

Rebuild the web UI's right panel as the upstream panel: one tab strip with a New-tab menu, a launcher while no tab is open, and a Close control. The fork's floating panel, its drill stack and its back-chevron navigation are gone.
