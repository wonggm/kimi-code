---
"@moonshot-ai/kimi-code": patch
---

Web: a session title that does not fit now fades at its edge instead of ending in an ellipsis, and scrolls while the pointer is on the row so the hidden part can be read.
