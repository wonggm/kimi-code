---
"@moonshot-ai/kimi-code": patch
---

Web: the Settings model menu groups models by provider with the current model's provider first, pins a More models entry that opens the model window, and the model window itself follows the reference build's layout.
