---
"@moonshot-ai/kimi-code": patch
---

Fix the web UI right panel: the top edge sits flush with the chat header, and widening into a subagent view animates smoothly instead of pausing then jumping in Firefox.
