---
"@moonshot-ai/kimi-code": patch
---

Web: the header's git pills take a translucent lift so they blend with the floating header pill, the right panel shares the transcript's ground instead of painting its own, the panel animates closed the way it animates open, and the sidebar toggle centres on the floating header pill.
