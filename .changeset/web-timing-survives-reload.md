---
"@moonshot-ai/kimi-code": patch
---

Web: an exchange's total time and a thinking block's span still read the same after a page reload and after switching sessions, taken from the session's transcript instead of only from the live stream.
