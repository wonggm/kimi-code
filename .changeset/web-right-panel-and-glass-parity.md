---
"@moonshot-ai/kimi-code": patch
---

Web: the sidebar mascot uses upstream's blue in the dark theme, the right panel's open control is upstream's icon with its hover animation, its tail buttons show tooltips again, Expand panel now widens the panel as upstream does, a click on a tab no longer closes it, the empty panel's launcher rows match upstream, and the liquid glass toggle now takes the modal scrim and the settings controls to upstream's rendering when it is off.
