---
"@moonshot-ai/kimi-code": patch
---

Web: a subagent's session in the right panel renders as a turn transcript like the main conversation instead of a stack of flat rows, and expanding that panel takes the whole area instead of squashing the conversation beside it.
